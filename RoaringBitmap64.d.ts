import roaring from ".";

/**
 * Roaring bitmap that supports 64 bit unsigned integers via BigInt.
 *
 * See http://roaringbitmap.org/
 *
 * @type {roaring.RoaringBitmap64}
 */
export = roaring.RoaringBitmap64;
